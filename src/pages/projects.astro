---
import Layout from "../layouts/Layout.astro";

const projects = [
    {
        title: "NEM: Notre Education Moderne",
        description:
            "An online education platform that helps students learn topics and languages globally, while giving teachers full control over scheduling and class formats.",
        tags: ["SvelteKit", "TypeScript", "Prisma", "Postgres", "Stripe", "Go"],
        publishDate: "July 20 2023",
        link: "/projects/nem-notre-education-moderne",
    },
    {
        title: "Deadale",
        description:
            "An app that translates one ad video into multiple localized versions with customizable voices and accents to help teams scale global reach quickly.",
        tags: ["Next.js", "TypeScript", "Postgres", "Stripe", "Go", "OpenAI"],
        publishDate: "June 12 2023",
        link: "/projects/deadale",
    },
];
---

<Layout
    title="Projects — Eliott Wantz"
    description="Selected projects and open-source work."
>
    <!-- Header -->
    <section class="pt-36 pb-12 px-6">
        <div class="max-w-2xl mx-auto">
            <h1
                class="font-display text-5xl sm:text-6xl md:text-7xl font-light text-cream/90 animate-fade-up"
            >
                Projects
            </h1>
            <p
                class="mt-5 font-display italic text-xl text-muted animate-fade-up"
                style="animation-delay: 0.12s;"
            >
                Things I&rsquo;ve brought to life
            </p>
        </div>
    </section>

    <!-- Grid -->
    <section class="max-w-3xl mx-auto px-6 pb-32">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
            {
                projects.map((project, i) => (
                    <a
                        href={project.link}
                        class="group relative block p-8 border border-rule rounded-sm bg-surface/40
                   hover:border-amber/20 hover:bg-elevated/50
                   transition-all duration-500
                   hover:shadow-[0_0_50px_-12px_rgba(200,150,60,0.07)]"
                        data-reveal
                        data-delay={String(i + 1)}
                    >
                        <h2 class="font-display text-2xl text-cream/90 group-hover:text-amber transition-colors duration-300 font-light">
                            {project.title}
                        </h2>
                        <p class="mt-3 text-xs tracking-[0.2em] uppercase text-faded">
                            {project.publishDate}
                        </p>
                        <p class="mt-4 text-[16px] text-muted leading-[1.85]">
                            {project.description}
                        </p>
                        <div class="mt-6 flex flex-wrap gap-2">
                            {project.tags.map((tag) => (
                                <span class="text-xs tracking-[0.18em] uppercase text-faded/80 border border-rule/50 px-2.5 py-1 rounded-sm">
                                    {tag}
                                </span>
                            ))}
                        </div>
                    </a>
                ))
            }
        </div>
    </section>
</Layout>
